---
interface Props {
  institution: string;
  role: string;
  paragraphs: string[];
  startYear: string;
  endYear: string;
}

const { institution, role, paragraphs, startYear, endYear } = Astro.props;
---

<article>
  <div class="experience--header">
    <h3>{institution}</h3>
    <p set:html={`${startYear} - ${endYear}`} />
  </div>
  <h4 set:html={role} class="experience--role" />
  <div>
    {paragraphs.map((paragraph) => <p set:html={paragraph} />)}
  </div>
</article>

<style>
  .experience--header {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .experience--role {
    margin-bottom: 1rem;
  }
</style>

<script>
  import { showAnnotations } from "@/utils/roughNotation";

  document.addEventListener("DOMContentLoaded", () => {
    const underline = document.querySelectorAll("#underline");
    const nodes = Array.from(underline);

    showAnnotations(nodes, "underline");
  });
</script>
